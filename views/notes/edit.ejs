<!DOCTYPE html>
<html>
<head>
  <title>Edit Note</title>
  <link rel="stylesheet" type="text/css" href="/style.css">
</head>
<body>
  <header>

    <div class="header-title">
      <h1>Swift Note</h1>
        <span>Keep Your Notes Here!</span>
    </div>
  </header>
  <main>
  <% if (typeof error !== 'undefined') { %>
    <p style="color:red;"><%= error %></p>
  <% } %>
  <div class="form-container">
    <p>Edit note</p>
    <form action="/<%= note._id %>" method="POST">
      <label for="title">Title:</label>
      <input type="text" id="title" name="note[title]" value="<%= note.title %>" required>
      <label for="content">Content:</label>
      <textarea id="content" name="note[content]" required><%= note.content %></textarea>
      <button class="note-action" type="submit">Update Note</button>
    </div>
    </form>
  </div>
  <a class="go-back" href="/">Back to Notes</a>
</main>
</body>
</html>
